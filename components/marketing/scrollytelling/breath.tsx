/**
 * A deliberate empty beat between cinematic acts — pure black, no text, no
 * motion. Scenes that cut straight into each other read as busy; a short
 * decompression zone gives the next one room to land.
 */
export function Breath() {
  return <div className="h-[12vh] bg-[#020203] md:h-[16vh]" aria-hidden />;
}
